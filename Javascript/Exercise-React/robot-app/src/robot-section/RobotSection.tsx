import React, { FunctionComponent } from "react";

interface RobotProps {
    cars: R
}

const RobotSection: FunctionComponent<RobotProps> = (cars) => {

    return (
        <h1>faw</h1>
    )
};

export default RobotSection;